---
name: "Checks"
on:
  push:
    branches:
      - main
  pull_request: {}
jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup
      - uses: pre-commit/action@v3.0.1
